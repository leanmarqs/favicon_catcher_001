# Variáveis de ambiente para desenvolvimento

# URL de conexão do MySQL (ajuste usuário, senha, host, porta e banco)
# Observação: se a senha tiver caracteres especiais, use URL encoding
# Ex.: senha "Answer=42" vira "Answer%3D42"
DATABASE_URL="mysql://root:password@localhost:3306/default_db"

# Porta do servidor HTTP
PORT=4000

